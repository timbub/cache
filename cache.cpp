bool update_cache(int page, cache_t* cache)
{
    auto it1 = cache->hash_id.find(page);
    if(it1 == cache->hash_id.end())
    {
        if(full
    } else
    {
       auto it2 = cache->hash_fr.find(it1->second.freq);

    }
}
