#include <gtest/gtest.h>
#include <cstdlib>

template <typename T>
struct test_t
{
    size_t size_cache;
    size_t num;
    std:vector<T> data;
    size_t hits;

};


TEST(EndToEnd, In_Out)
{
    test_t tests[] = {

        
    }
    srd::string = pr_path + "<test1.in >test1.out
    int std::system
}

int main(int argc, char* [])
{
    ::testing::InitGoogleTests(&argc, argv);
    pr_path = argv[1];
    return RUN_ALL_TESTS();
}
